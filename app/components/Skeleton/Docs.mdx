import { Meta, Primary, Controls, Story, Primary } from '@storybook/blocks';
import * as SkeletonStories from './Skeleton.stories';

<Meta of={SkeletonStories} />

# Skeleton

任意のサイズのSkeletonを表示するコンポーネントです。
ユーザーに表示するコンテンツが読み込み中であることを伝えるために利用します。

<Primary />

## Props

<Controls />
